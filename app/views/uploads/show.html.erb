<%= form_tag(home_path, method: :get) do %>
  <%= submit_tag ("Back"), class: 'back-button btn btn-outline-dark'%>
<% end %>
<div class="">
    <%= image_tag @upload.image if @upload.image.attached? %>


    <div class="">
        <h1 class="">
            <%= link_to @upload.title, @upload %>
        </h1>

    <div class="">
        <%= @upload.description %>
    </div>
    </div>
</div>

